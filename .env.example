# Backend API base URL (no trailing slash, must include /api prefix)
VITE_API_URL=http://localhost:5000/api

# Backend base URL for WebSocket connection (no trailing slash, no /api)
VITE_WS_URL=http://localhost:5000
